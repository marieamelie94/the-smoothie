
<%= @smoothie.name %>
<%= @smoothie.nickname %>
<%= @smoothie.description %>

Ingredients:
<% @smoothie.doses.each do |d| %>
  <%= d.ingredient.name %> (<%= d.grams%><%= d.mililiters%><% if d.grams %> grams)<% else %> mililiters)<% end %>
<% end %>

<%= link_to 'Back to Smoothies', smoothies_path, class: 'btn btn-default'  %>
