<div class="container">
  <div class="smoothies">
    <% @smoothies.each do |s| %>
      <div>
        <%= render 'smoothie', { s: s } %>
      </div>
    <% end %>
  </div>
  <div class="btn btn-center">  <%= link_to 'Get Started', page_path("choose-amount"), class: "btn btn-lg btn-success b1" %>
  </div>
</div>
